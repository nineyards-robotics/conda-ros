[workspace]
name = "conda-ros"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.11"
pyyaml = "*"
rattler-build = "*"

[tasks]
generate-recipes = "python -m pipeline.generate_recipes"
